# Excel-Assistant
A simple, lightweight Excel assistant that watches selected columns or rows and notifies users when tracked dates hit a defined threshold. Supports automatic email alerts with configurable recipients and message content.
